(function ()
{
    var app = angular.module('app.controllers', ['ngCookies', 'angularUtils.directives.dirPagination']);

    app.controller('HomeCtrl', ['$scope', '$location', '$window', '$http', '$cookieStore', function ($scope, $location, $window, $http, $cookieStore) {
           
        }]);
});